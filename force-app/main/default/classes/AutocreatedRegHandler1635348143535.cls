//TODO:This autogenerated class includes the basics for a Registration
//Handler class. You will need to customize it to ensure it meets your needs and
//the data provided by the third party.

global class AutocreatedRegHandler1635348143535 implements Auth.RegistrationHandler{

global User createUser(Id portalId, Auth.UserData data)
{
user u  = [Select Id from User where federationidentifier = :data.email Limit 1];
System.debug('what email received'+data.email);
System.debug('user is-->'+u);
return u;
}
global void updateUser(Id userId, Id   portalId, Auth.UserData data)
{
System.debug('Update Method called--user'+userId);
System.debug('User data from auth is-->'+data);
}
}