trigger MSD_CORE_Comment_Trigger on MSD_CORE_Comments__c (after insert) {
  /*if(trigger.isinsert && trigger.isAfter){
      if(MSD_CORE_KnowledgeTriggerHandler.executeTrigger)
        MSD_CORE_CommentTriggerHandler.UpdateConverstions(trigger.new);
        
    }*/
  }